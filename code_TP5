### Estimation non-paramétrique de la fonction de régression

# fonction
f = function(x){
  f = x*x - 1
}

# erreur quadratique
# mse(y, y^)

# Simulation
sig2 = 0.25
n = 1000
x = rnorm(n, mean=0,sd=1)
y = data.frame(f(x))[,] + rnorm(n, mean=0, sd=sig2)

